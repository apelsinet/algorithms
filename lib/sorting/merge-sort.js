/**
 * ### Merge-sort algorithm.
 * @module sorting/merge-sort
 */

/**
 * ##### Complexity:
 * | Case     | Time       | Space |
 * |:---------|:-----------|:------|
 * | Best:    | O(n log n) |       |
 * | Average: | O(n log n) |       |
 * | Worst:   | O(n log n) | O(n)  |
 *
 * @function
 * @param {Object[]} arr - The input array.
 * @returns {Object[]} Sorted array (ascending).
 */
module.exports = (arr = []) => {


  /** Return the sorted array. */
  return arr;

}
